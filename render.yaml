services:
  - type: web
    name: jarvis-app
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: CEREBRAS_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
